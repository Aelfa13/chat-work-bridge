# Security Design

Draft placeholder. Security claims must be tied to enforceable controls rather than prompts.

Required controls include canonical path validation, symlink-escape rejection, fixed read-only executor mode, minimal inherited environment, loopback binding, local authentication, bounded resources, and redaction before persistence.
