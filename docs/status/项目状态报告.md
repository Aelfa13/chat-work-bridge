# chat-work-bridge 项目状态报告

基线日期：2026-08-31
治理等级：continuous

## 项目事实

- Node.js / TypeScript local MCP STDIO package，Node.js 22+。
- 构建：`npm run build`；测试：`npm test`。
- GitHub CI/release 已存在：`.github/workflows/ci.yml`、`RELEASE_NOTES.md`。
- `main` 是 pure upstream-sync baseline；`dev` 是治理与个人集成线。
- `feature/windows-chatgpt-desktop` 是计划中的 Windows 适配分支，当前尚未创建。
- `origin` 是个人 fork，`upstream` 是原项目；两者 SSH `main` actual proof 已通过。
- 当前没有真实 production runtime；validation 只用于独立开发测试，不是 production copy。
- Git writes 默认 `MANUAL`；本次不修改 main 或 upstream。

## Windows port baseline

| 项目 | 值 |
|---|---|
| WINDOWS_PORT_BASELINE_STATUS | ACCEPTED_WITH_KNOWN_PLATFORM_FAILURES |
| Full test role | SUPPLEMENTAL |
| TOTAL / PASS / KNOWN_FAIL / SKIP | 331 / 244 / 86 / 1 |
| UNEXPLAINED_FAILURES | 0 |
| UPSTREAM_REGRESSION | NOT_PROVEN |
| Failure classes | PATH_SEMANTICS=58；EOL_CONFIGURATION=26；SYMLINK_PERMISSION=2 |

Windows baseline 不是 331/331 PASS gate；已知平台失败不得直接描述为业务 bug。

## 总状态

| 项目 | 状态 | 证据/下一步 |
|---|---|---|
| Git repository | PASS | Git 已存在；本次未初始化 |
| Git transport proof | PASS | origin/upstream 为 SSH；两个 `main` actual proof 已通过 |
| Upstream baseline | PASS | `main` 精确同步 `upstream/main`；`origin/main` 为个人 fork 镜像 |
| Dev upstream integration | PASS | `dev` 保留原 governance commit，并已合入最新 `main` |
| Windows known baseline | PASS | 331/244/86/1，0 unexplained；状态为 accepted known platform failures |
| Windows port regression gate | MANUAL | 后续 targeted Windows validation；不得增加 unexplained failure |
| Validation boundary | MANUAL | 独立开发测试边界已记录，默认不启动；尚未做 start → test → stop rehearsal |
| GitHub CI/release | PASS | `.github/workflows/ci.yml` 与 GitHub Release 事实已存在 |
| Production runtime | N/A | 当前没有真实 production runtime |
| Runtime data backup | N/A | 当前没有 production runtime；若未来产生不可重建状态再配置 |
| Private config backup | NOT_CONFIGURED | 本次未读取或记录 secrets；未来按需配置加密恢复 |
| Recovery mirror | NOT_CONFIGURED | 未配置 recoverable mirror |
| Read-only inspection | NOT_CONFIGURED | 建立后先观察再考虑写自动化 |
| Production closure | N/A | 当前没有 production runtime；未来进入生产前完成比较契约和回滚门 |

## 已完成

- continuous 治理文件已生成并按当前项目事实适配。
- `main`、`origin/main`、`upstream/main` 已处于同一 upstream v1.4.0 baseline。
- `dev` 已保留原 governance commit 并合入 main；本次只更新精确治理文件，不修改业务树。
- Windows known baseline、三层 gate、branch model 和 upstream CI contract 已记录。
- origin/upstream 角色、SSH proof、validation 边界和 production N/A 已记录。

## 未完成/风险

- [ ] Windows port targeted validation 尚未建立；feature/windows-chatgpt-desktop 仍未创建
- [ ] Validation 独立 workspace 的 start → test → stop rehearsal
- [ ] 分支保护与评审规则
- [ ] recoverable mirror（如需要）
- [ ] 运行数据备份（仅在未来产生不可重建状态时）
- [ ] 私密配置加密备份与恢复演练（仅在未来需要私密配置时）
- [ ] Production 发布/回滚/closure（当前 N/A）
- [ ] `.gitignore` 通用私钥扩展 gap（本次仅记录，未修改）
