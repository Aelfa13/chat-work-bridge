# Contributing

The project is in pre-alpha design and is not accepting implementation pull requests yet.

Before implementation begins, contributions must preserve the frozen v0.1 scope, avoid machine-specific paths or private integrations, and include tests for every security boundary they affect.
